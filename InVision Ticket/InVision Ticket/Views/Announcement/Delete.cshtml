@model InVision_Ticket.Models.Announcement

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Announcement</legend>

    <div class="display-label">HTML</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.HTML)
    </div>

    <div class="display-label">Markup</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Markup)
    </div>

    <div class="display-label">CreatedDateTime</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.CreatedDateTime)
    </div>

    <div class="display-label">CreatedByLoginID</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.CreatedByLoginID)
    </div>

    <div class="display-label">DirectHTML</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.DirectHTML)
    </div>

    <div class="display-label">CSS</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.CSS)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
