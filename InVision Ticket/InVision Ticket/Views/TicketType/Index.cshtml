@model IEnumerable<InVision_Ticket.Models.TicketType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            TicketType1
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TicketType1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TicketTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TicketTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TicketTypeID })
        </td>
    </tr>
}

</table>
