@model InVision_Ticket.Models.TicketType

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>TicketType</legend>

        @Html.HiddenFor(model => model.TicketTypeID)

        <div class="editor-label">
            @Html.LabelFor(model => model.TicketType1)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TicketType1)
            @Html.ValidationMessageFor(model => model.TicketType1)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
