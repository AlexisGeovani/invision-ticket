@model InVision_Ticket.Models.Customer

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Customer</legend>

    <div class="display-label">BusinessCustomer</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.BusinessCustomer)
    </div>

    <div class="display-label">CustomerName</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.CustomerName)
    </div>

    <div class="display-label">CustomerNotes</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.CustomerNotes)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
